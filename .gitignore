# ── The brain (personal data) — lives in-repo, never committed ──
/wiki/
/inbox/
/.raw/
/_templates/
/.obsidian/
/TASKS.md

# Vault runtime state (indexes, locks, transport/mode detection)
/.vault-meta/

# Session archives (may contain sensitive transcripts)
/archive/

# Research material — reference git histories (3.6G) and old templates
/repos/
/_reference/

# Personal Claude Code overrides (shared settings.json IS committed)
/.claude/settings.local.json

# OS / tooling noise
.DS_Store
__pycache__/
*.pyc
